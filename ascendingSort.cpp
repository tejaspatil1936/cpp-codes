#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cout << "Enter the no of elements in array : ";
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < n - 2; i++)
    {
        int mini = i;
        for (int j = 0; j < count; j++)
        {
            /* code */
        }
        
    }
    
    return 0;
}