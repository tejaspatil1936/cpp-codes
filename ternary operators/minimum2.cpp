#include <bits/stdc++.h>
using namespace std;

int main()
{
    float a, b;
    cout << "Enter a :";
    cin >> a;
    cout << "Enter b :";
    cin >> b;
    cout << (a > b ? "b is minimum \n" : "a is minimum \n");


    return 0;
}