#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    cout << "Enter the number : " << endl;
    cin >> a;
    cout << a << (a > 0 ? " is positive \n" : " is negative \n");
    return 0;
}